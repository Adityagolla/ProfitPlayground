name: tests

on:
  push:
  pull_request:

jobs:
  matching-engine:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and run matching engine test suite
        run: make test
